source "https://rubygems.org"

ruby File.read(File.join(__dir__, '.ruby-version')).strip rescue '3.4.1'

gem 'jekyll', '~> 4.3'
# Theme
gem 'minima', '~> 2.5'
# Markdown processor
gem 'kramdown', '>= 2.4'
# Syntax highlighting
gem 'rouge', '>= 4.2'
# Live reload
 gem 'webrick', '~> 1.8'

# Uncomment to use GitHub Pages (locks versions):
# gem 'github-pages', group: :jekyll_plugins

group :jekyll_plugins do
  gem 'jekyll-feed', '~> 0.17'
end
